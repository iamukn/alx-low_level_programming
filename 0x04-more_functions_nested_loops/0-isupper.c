#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int main(void)
{
int c, a, z;

a = 'A';
b = 'Z';
c = _isupper(c);

if (c >= a || c <= b )
{
return (1);
}
else 
{
return (0);
}
return (0);
}
