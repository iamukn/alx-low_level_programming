#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies 2 values;
 *
 * @a:holds one value
 *
 * @b: holds value to be multipled
 *
 * Return: always 0;
 */

int mul(int a, int b)
{
return (a * b);
}
